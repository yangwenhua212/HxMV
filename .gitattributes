# 统一行尾：仓库内一律 LF，检出时由各平台自行决定。
# 为什么需要它：Windows 协作者（core.autocrlf=true）与 Linux/macOS 协作者
# （autocrlf=false）对同一个文件会算出不同的哈希，diff 里出现整文件"改动"。
# 声明成 text=auto 后，git 在提交时统一规范化，这类假 diff 从根上消失。
* text=auto eol=lf

# 二进制资产：不要做任何行尾/编码转换（否则会损坏文件）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.mp4 binary
*.svg text eol=lf

# 明确按文本处理、且必须是 LF 的脚本（含 shebang，行尾错了在 Linux 上跑不了）
*.sh text eol=lf
*.py text eol=lf
